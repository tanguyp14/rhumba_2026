(function($) {
    'use strict';

    // Initialisation du bloc Nouveautés
    const initRhumbaNewBlock = function() {
        console.log('Rhumba Nouveautés block initialized');
    };

    // Init on DOM ready
    $(document).ready(function() {
        initRhumbaNewBlock();
    });

})(jQuery);
